settings:
  always_yes: false
  do not show groups at startup: false
  interval for checking updates: 180
  search:
    match_all: true
    newest_only: true
  show updates at startup: false
view:
  filter: all
  show: all
