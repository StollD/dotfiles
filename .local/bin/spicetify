#!/bin/sh

SPOTIFY="/var/lib/flatpak/app/com.spotify.Client/x86_64/stable/active/files/extra/share/spotify"

if [ ! "$(stat -c "%a" $SPOTIFY)" = "777" ]; then
	sudo chmod -R 777 $SPOTIFY
fi

$HOME/.local/share/spicetify/spicetify $@
