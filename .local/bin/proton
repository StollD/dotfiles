#!/bin/sh

if [ "$PROTONPREFIX" = "" ]; then
	PROTONPREFIX="$HOME/.proton"
fi

export STEAM_COMPAT_DATA_PATH="$PROTONPREFIX"
mkdir -p "$PROTONPREFIX"

if [ -f "$PROTONPREFIX/pfx.lock" ]; then
	rm "$PROTONPREFIX/pfx.lock"
fi

$HOME/.local/share/proton/proton run $@
